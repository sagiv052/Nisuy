# חיבור הבוט ל‑Render ול‑Cloudinary

## מה השתנה בקוד

כאשר מוגדר `CLOUDINARY_URL`, כל קובץ חדש שהבוט מקבל מורד זמנית מ‑Telegram ומועלה ל‑Cloudinary באמצעות העלאה חתומה ומדורגת. כתובת Cloudinary נשמרת בקטלוג. אם ההעלאה נכשלת, הבוט נשאר פעיל ומשתמש אוטומטית בקישור הסטרימינג הקיים מ‑Telegram.

Cloudinary משמש כאן לאחסון **קבצי המדיה** בלבד. הוא אינו מסד נתונים, ולכן אינו שומר את `catalog.db` באופן שמאפשר שאילתות ועדכונים אמינים.

## הגדרה ב‑Render

ב‑Render פותחים את השירות של הבוט, נכנסים ל‑**Environment → Environment Variables**, ומוסיפים:

```text
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_FOLDER=telegram-stream-bot
```

אין להכניס את הערכים האלה ל‑Git או לקובץ `.env` שמעלים לפרויקט. אין צורך ב‑Upload Preset לא־חתום עבור החיבור הזה: השרת משתמש ב‑API Secret ומבצע העלאה חתומה. ה‑Upload Preset הלא־חתום מיועד להעלאות ישירות מהדפדפן או מ‑Upload Widget.

לאחר שמירת המשתנים מבצעים **Manual Deploy**. בכל קבלת קובץ חדש יש להמתין לסיום ההעלאה לפני שימוש בקישור שנשמר בקטלוג.

## שמירת הקטלוג בין פריסות

ב‑Render Free מערכת הקבצים המקומית זמנית, ולכן `catalog.db` עלול להימחק בכל פריסה או הפעלה מחדש. האפשרויות הבטוחות הן:

1. להעביר את הקטלוג למסד נתונים חיצוני, למשל PostgreSQL, ולשנות את שכבת `Catalog` בהתאם.
2. להשתמש בשירות Render בתשלום עם Persistent Disk ולכוון את `CATALOG_DB` לנתיב שעל הדיסק.

החיבור ל‑Cloudinary לבדו פותר את מחיקת קבצי הווידאו החדשים, אבל **לא** את מחיקת הרשאות המשתמשים, הסדרות, הפרקים ונתוני הקטלוג.

## אבטחה

ה‑API Secret שהופיע בהודעת ההגדרה חשוף. יש להיכנס ל‑Cloudinary, לבטל את המפתח הישן או ליצור זוג API חדש, ורק אז להגדיר את `CLOUDINARY_URL` ב‑Render. לאחר ההחלפה אין לשמור את הסוד בצ'אט, ב‑Git או בקובץ ZIP.
