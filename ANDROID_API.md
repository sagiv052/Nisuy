# חוזה אינטגרציה לאפליקציית הסרטים והסדרות

הארכיטקטורה המומלצת היא שהאפליקציה תמשוך metadata מהשרת, ותבקש את כתובת הסטרימינג רק כאשר המשתמש לוחץ על כפתור הצפייה. כך מסך הבית נשאר מהיר, והשרת אינו מייצר בקשות סטרימינג מיותרות.

## זרימת שימוש

1. האפליקציה קוראת ל־`GET /api/v1/catalog` ומציגה פוסטר, כותרת, סוג, שנה, דירוג ותקציר קצר.
2. המשתמש לוחץ על פריט, והאפליקציה קוראת ל־`GET /api/v1/catalog/{id}`.
3. עבור סרט, הצגת כפתור צפייה קוראת ל־`GET /api/v1/catalog/{id}/play`.
4. עבור סדרה, הצגת מסך העונות והפרקים משתמשת במערכים `seasons` ו־`episodes`.
5. לחיצה על פרק קוראת ל־`GET /api/v1/catalog/{series_id}/episodes/{episode_id}/play`.
6. השרת מחזיר `stream_url`, והאפליקציה מעבירה אותו ל־ExoPlayer או ל־Media3.
7. הנגן משתמש ב־HTTP Range של השרת לצורך Seek, עצירה והמשך.

## דוגמאות תגובה

### רשימת קטלוג

```http
GET /api/v1/catalog?kind=movie&page=1&limit=30
```

```json
{
  "api_version": 1,
  "page": 1,
  "limit": 30,
  "total": 1,
  "items": [
    {
      "id": 12,
      "kind": "movie",
      "title": "שם הסרט",
      "summary": "תקציר",
      "release_year": 2026,
      "poster_url": "https://...",
      "backdrop_url": "https://...",
      "rating": 7.8
    }
  ],
  "summary": {
    "movies": 1,
    "series": 0,
    "seasons": 0,
    "episodes": 0,
    "uploads": 1
  }
}
```

### פרטי סדרה

```http
GET /api/v1/catalog/12
```

התגובה כוללת `item`, `seasons` ו־`episodes`. רשומות הפרקים אינן כוללות קישור סטרימינג מראש; הקישור מתקבל רק מ־endpoint ה־`play` לאחר בחירת הפרק.

### קבלת קישור לסרט

```http
GET /api/v1/catalog/12/play
```

### קבלת קישור לפרק

```http
GET /api/v1/catalog/12/episodes/55/play
```

קוד HTTP שימושי לאפליקציה:

| קוד | משמעות | פעולה באפליקציה |
|---:|---|---|
| 200 | הצלחה | לפתוח את הנגן עם `stream_url` |
| 400 | בקשה לא מתאימה | לתקן את הבקשה או להציג שגיאה כללית |
| 404 | פריט או פרק לא קיימים | לרענן קטלוג ולהסיר פריט לא זמין |
| 409 | הפריט קיים אך עדיין אין לו קובץ | להציג “התוכן עדיין בעיבוד” |
| 429 | עומס זמני | להמתין ולנסות שוב עם backoff |
| 503 | השירות או קיבולת הסטרימינג עמוסים | להציג retry ולא לקרוס |

## המלצות Android

להשתמש ב־Media3/ExoPlayer עם `MediaItem.fromUri(streamUrl)`, לא להוריד את הקובץ מראש ולא לנסות לתרגם את כתובת Telegram בעצמך. להגדיר timeout, retry policy, שמירת מיקום צפייה מקומית ומסך שגיאה עם כפתור “נסה שוב”.

לשמור את כתובת ה־API בהגדרת build נפרדת, למשל `BuildConfig.API_BASE_URL`, ולא לפזר אותה בקוד. להשתמש ב־Retrofit או Ktor Client, במודלי DTO נפרדים וב־Repository אחד שמנהל cache ורענון.

כדאי להשתמש ב־`ETag` שמוחזר מרשימת הקטלוג. אם הקטלוג לא השתנה, האפליקציה יכולה להשתמש בנתונים הקיימים ולא להוריד שוב את כל הפוסטרים והכותרות.

## סדר המימוש באפליקציה

להתחיל במסך קטלוג עם סרטים וסדרות, לאחר מכן מסך פרטים, אחריו מסך עונות ופרקים, ורק אז נגן. אין לחבר את הנגן ישירות לנתוני Telegram; האפליקציה צריכה לעבוד מול חוזה ה־API בלבד.

## Playback מתקדם ל־Direct ו־TS

לקבצים גדולים או לרשת לא יציבה משתמשים ב־`GET /api/v1/playback/{chat_id}/{message_id}?mode=auto`. השירות מחזיר קישור Direct עם HTTP Range בלי להוריד את הקובץ לשרת ובלי להפעיל FFmpeg. Media3/ExoPlayer יכול לבצע Seek באמצעות בקשות Range חדשות.

הנגן צריך לתמוך גם ב־`video/mp2t` עבור קובצי TS. HLS לא מופעל בגרסה זו, משום שהוא דורש הורדה, אחסון והמרה בצד השרת. אם בעתיד יהיה שרת חזק או CDN, אפשר להוסיף HLS כשירות נפרד.
