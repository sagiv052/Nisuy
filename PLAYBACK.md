# Playback קל לשרת חינמי

השרת משתמש בסטרימינג ישיר מ־Telegram עם HTTP Range. אין הורדה זמנית של קובץ, אין FFmpeg ואין המרת HLS בצד השרת. כך צריכת הדיסק וה־CPU נשארת נמוכה ומתאימה להרצת בדיקות מקומית ולשרת חינמי.

| מצב | מתי משתמשים | אחורה/קדימה |
|---|---|---|
| Direct Range | MP4, MKV, WebM וקבצים רגילים | נתמך דרך HTTP Range |
| MPEG-TS | קובץ שמגיע כ־`.ts` או MIME של `video/mp2t` | נתמך דרך HTTP Range; איכות ה־Seek תלויה במבנה הקובץ |

## API לבחירת Playback

```http
GET /api/v1/playback/{chat_id}/{message_id}?mode=auto
```

ה־API מחזיר URL ישיר, מידע על גודל הקובץ, MIME והאם Seek נתמך:

```json
{
  "api_version": 1,
  "source": {
    "size": 734003200,
    "mime_type": "video/mp4"
  },
  "direct": {
    "url": "https://server.example/stream/123/456",
    "supports_range": true,
    "supports_seek": true
  },
  "selected": {
    "mode": "direct",
    "url": "https://server.example/stream/123/456"
  }
}
```

הפרמטר `mode` יכול להיות `auto` או `direct`. שני המצבים משתמשים באותו stream קל. מצב HLS אינו מופעל בגרסה הזו, משום שהוא דורש הורדת הקובץ לשרת, אחסון זמני ו־FFmpeg.

## אחורה וקדימה

השרת מטפל בבקשות `Range` של הנגן, כולל טווחים פתוחים וטווחי suffix. לכן Media3/ExoPlayer יכול לבקש מקטעים שונים של הקובץ בזמן Seek, בלי שהשרת יוריד את כל הקובץ מההתחלה.

באפליקציה יש להשתמש ב־Media3/ExoPlayer, להציג טעינה בזמן בקשת Range, ולאפשר retry במקרה של ניתוק. עבור קובץ TS, הנגן צריך לקבל את ה־MIME שהשרת מחזיר (`video/mp2t`) ולאלץ HLS רק אם יש playlist אמיתי — לא להפוך קובץ TS ל־HLS בצד השרת.

## למה לא HLS כרגע

HLS מתאים לפרודקשן עם CDN, דיסק מתמיד ותהליך transcoding נפרד. בשרת חינמי, המרה של קובץ גדול עלולה לצרוך CPU, זיכרון, דיסק וזמן, ואף לגרום לשירות להיסגר. לכן הבחירה הנכונה כרגע היא Direct Range יעיל. אם בעתיד יהיה שרת חזק או CDN, ניתן להוסיף HLS כשירות נפרד בלי לשנות את חוזה האפליקציה.

## מטמון RAM ללא דיסק

כדי לשפר חזרה לאחור וניסיונות חוזרים, השרת שומר רק מקטעים חמים בזיכרון RAM. במפרט Render Free של 0.1 CPU ו־512MB RAM המטמון מוגבל ל־32MB כברירת מחדל, מוחק פריטים ישנים לפי LRU ו־TTL, ומונע הורדה כפולה של אותו מקטע כאשר כמה בקשות מגיעות במקביל. הוא אינו שומר סרט שלם ואינו כותב קבצים לדיסק.

```env
STREAM_CACHE_MB=32
STREAM_CACHE_TTL=90
STREAM_READ_AHEAD=0
```

`STREAM_READ_AHEAD=0` הוא ברירת המחדל המומלצת לשרת חינמי. אם יש מספיק רוחב פס ורוצים מעט buffering נוסף, אפשר להגדיר `1`, אך זה יגרום להורדה מוקדמת של מקטע אחד נוסף.
